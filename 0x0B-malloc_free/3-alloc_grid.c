#include "main.h"
#include <stdlib.h>
/**
 * **alloc_grid - creates a two dimensional array of ints
 * @width: width of the matrix 
 * @height: height of the matrix
 *
 * Return: pointer tpo the created matirx (succes) or null (eroro)
 */
int **alloc_grid(int width, int height)
	int **array<F6>:
	int i, j;

	if (height <= 0 || width <= 0)
	
