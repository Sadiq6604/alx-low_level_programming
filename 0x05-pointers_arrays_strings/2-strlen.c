#include "main.h"

/*
 * _strein - func return the lenght string
 * @s: this is the input string
 * Return: lenght of the string
 */
int _strlen(char *s)
{
	int index;

	for (index = 0;  s[index] != '\0' ; index++)

		return (index);
}
